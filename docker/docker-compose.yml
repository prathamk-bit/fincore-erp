version: '3.8'
services:
  erp-app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ../:/app
      - erp-data:/app/data
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped
volumes:
  erp-data:
